def somar(a, b):
    somando = float(a) + float(b)
    return somando
def subtrair(a , b):
    subtraindo = float(a) - float(b)
    return subtraindo
def multiplicar(a, b):
    multiplicando = float(a) * float(b)
    return multiplicando
def dividir(a, b):
    if b == 0:
        return 0
    dividindo = float(a) / float(b)
    return dividindo
def saida():
    print("... Você escolheu sair do programa ...")
    print("Saindo...")